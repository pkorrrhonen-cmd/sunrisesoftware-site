# --- OS / editor cruft ---
.DS_Store
Thumbs.db
*.swp
.idea/
.vscode/

# --- Node / build (for the future Vite + React migration) ---
node_modules/
dist/
.wrangler/
*.log
npm-debug.log*

# --- Local build artifacts & backups (kept on disk, not versioned) ---
*.zip

# --- Superseded working folder (originals preserved locally, not versioned) ---
Verkkosivujen tiedostot/

# --- Claude Code local session files ---
.claude/

# --- Mockup captures and media copies: reproducible with docs/mockups/shoot.py and build-look.py,
#     the page's own copies live in images/ and media/ ---
docs/mockups/*.png
docs/mockups/shots/
docs/mockups/media/masterforge-eq.mp4
docs/mockups/media/petri-cutout.webp
