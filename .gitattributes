# Normalize all text files to LF in the repository (consistent across Windows/Linux).
* text=auto eol=lf

# Binary assets — never apply EOL conversion.
*.png   binary
*.jpg   binary
*.jpeg  binary
*.webp  binary
*.ico   binary
*.gif   binary
*.zip   binary
*.woff  binary
*.woff2 binary
